{
  "name": "the-four-big-tests",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Four thoughtful assessments, with private saves and offline installation.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && npm run build"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.4",
    "jsdom": "^26.1.0"
  }
}
