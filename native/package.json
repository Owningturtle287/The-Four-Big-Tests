{
  "name": "four-big-tests-native",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ../scripts/build.mjs && node build.mjs",
    "android": "npm run build && cap sync android",
    "ios": "npm run build && cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "8.5.2",
    "@capacitor/android": "8.5.2",
    "@capacitor/ios": "8.5.2",
    "@capacitor/filesystem": "^8.0.0",
    "@capacitor/share": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.2",
    "esbuild": "^0.25.0"
  }
}
